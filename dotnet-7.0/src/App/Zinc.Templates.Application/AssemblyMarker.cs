namespace Zinc.Templates.Application
{
    /// <summary>
    /// A well-known class that makes it easy to get information about the assembly. For example:
    /// <c>var assemblyName = typeof(AssemblyMarker).Assembly.GetName().Name;</c>. It also
    /// comes in handy when you need to register all types from a specific assembly.
    /// </summary>
    public class AssemblyMarker
    {
    }
}
